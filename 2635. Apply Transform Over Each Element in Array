/**
 * @param {number[]} arr
 * @param {Function} fn
 * @return {number[]}
 */
var map = function(arr, fn) {
    var a=[]
    var i=0
    while(i<arr.length)
    {
        a[i]=fn(arr[i],i)
        i++;
    }
    return a;
};
