class Solution {
public:
    bool isPalindrome(int x) {
        int i=0,j=0;
       string s=to_string(x);
        for(i=0,j=s.length()-1;i<j;i++,j--)
        {
            if(s[i]!=s[j])
                return false;
        }
        return true;
    }
};
