/**
 * @param {number[]} arr
 * @param {Function} fn
 * @return {number[]}
 */
var filter = function(arr, fn) {
    var i=0
    var a=[]
    var j=0
    while(i<arr.length)
    {
        if(fn(arr[i],i))
        a[j++]=arr[i]
        i++;
    }
    return a;
};
