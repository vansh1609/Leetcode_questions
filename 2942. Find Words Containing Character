class Solution {
public:
    vector<int> findWordsContaining(vector<string>& words, char x) {
        vector<int> v;
        for(int i=0;i<words.size();i++)
        {
            string str=words[i];
            if(str.find(x)!=-1)
            v.push_back(i);
        }
        return v;
    }
};
